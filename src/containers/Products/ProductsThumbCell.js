import styled from 'styled-components'

export default styled.div`
    display: flex;
    align-items: center;
    width: 7rem;
    height: 7rem;
    margin: -1.3rem 0;
    overflow: hidden;
    border-radius: var(--border-radius);
`
