import styled from 'styled-components'

export default styled.div`
    display: flex;
    flex-wrap: wrap;
    max-width: 40rem;
    position: absolute;
    left: 5rem;
    right: 5rem;
    top: calc(5rem - var(--offset));
    margin: 0 auto;
`
