import React from 'react'
import styled from 'styled-components'

const Logo = styled.span`
    font-size: 2.4rem;
    font-weight: 700;
    text-transform: uppercase;
`

export default () => <Logo>g-point</Logo>
